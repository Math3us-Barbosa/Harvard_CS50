import re

num = get_int("Number: ")
c = 0
//loop para checar validade do numero
while n > 0:
    if c % 2 != 0: //se impar
        y = 2 * (n % 10)
        if y >= 10:
            x = x + (y % 10) + (y / 10)
        else:
            x = x + y
        n = n / 10
    elif c % 2 == 0 //se par
        x = x + (n % 10)
        n = n / 10

    #declairing first 2 digits as p
    if n < 100 and n > 10:
        p = n
    c+=
if x % 10 != 0
    print("INVALID")
if p == 34 or p == 37
    print("Amex")
elif p =

