from cs50 import get_string

text = get_string("Text: ")
max = strlen(text)
word_counter = 1
letter_counter = 0
for i in range (max):
    if text[i] == ' ':
        word_counter  +=  1
    elif text[i]
        letter coun
